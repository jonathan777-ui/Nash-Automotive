{
  "name": "orbit-command-center",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Piece 1: persistent Operations Command Center. Checkpoint 1 scope only — the Cloudflare Access auth gate. The credential form and Secrets Store writes are checkpoint 2.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
